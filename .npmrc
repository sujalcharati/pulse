# Next.js 15 ships against React 19 RC, which trips strict peer resolution
# against optional peers declared by older libraries (e.g. drizzle's expo
# adapter). The legacy resolver here only relaxes peer warnings — exact
# version pinning still applies.
legacy-peer-deps=true
